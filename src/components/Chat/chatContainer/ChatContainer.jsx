import React from 'react';
import { StyledChatContainer } from './styledComponents';

const ChatContainer = () => {
	return (
		<>
			<StyledChatContainer />
		</>
	);
};

export default ChatContainer;
